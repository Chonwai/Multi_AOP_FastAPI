# Render Dashboard 配置指南（圖文版）

**日期**: 2024-12-13  
**適用於**: Multi-AOP FastAPI 項目

---

## 📋 配置步驟概覽

```
步驟 1: 創建 Web Service
    ↓
步驟 2: 連接 GitHub 倉庫
    ↓
步驟 3: 配置基本設置
    ↓
步驟 4: 配置 Docker 設置（🔴 關鍵）
    ↓
步驟 5: 配置環境變量
    ↓
步驟 6: 配置高級設置
    ↓
步驟 7: 部署！
```

---

## 步驟 1：創建 Web Service

1. 登錄 [Render Dashboard](https://dashboard.render.com)
2. 點擊 **"New +"** 按鈕（右上角）
3. 選擇 **"Web Service"**

```
┌─────────────────────────────────────┐
│  Render Dashboard                   │
│                                     │
│  [New +] ◄── 點擊這裡               │
│    │                                │
│    ├── Blueprint                    │
│    ├── Web Service  ◄── 選擇這個    │
│    ├── Private Service              │
│    ├── Background Worker            │
│    └── Cron Job                     │
└─────────────────────────────────────┘
```

---

## 步驟 2：連接 GitHub 倉庫

### 選項 A：首次連接 GitHub

1. 點擊 **"Connect GitHub"**
2. 授權 Render 訪問 GitHub
3. 選擇倉庫：`Multi_AOP_FastAPI`

### 選項 B：已連接 GitHub

1. 在倉庫列表中找到 `Multi_AOP_FastAPI`
2. 點擊 **"Connect"**

```
┌─────────────────────────────────────────────┐
│  Connect a repository                       │
│                                             │
│  🔍 Search repositories...                 │
│                                             │
│  ┌─────────────────────────────────────┐  │
│  │ chonwai-y/Multi_AOP_FastAPI         │  │
│  │ FastAPI microservice for AOP...     │  │
│  │                    [Connect] ◄──────┼──┼── 點擊這裡
│  └─────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

---

## 步驟 3：配置基本設置

### 3.1 服務名稱和區域

```yaml
┌────────────────────────────────────────┐
│ General                                │
├────────────────────────────────────────┤
│ Name: multi-aop-api                    │  ◄── 輸入服務名稱
│                                        │
│ Region: Singapore                      │  ◄── 選擇最近的區域
│         └─ [Oregon]                    │     （降低延遲）
│         └─ [Frankfurt]                 │
│         └─ [Singapore] ✓               │
└────────────────────────────────────────┘
```

### 3.2 分支和根目錄

```yaml
┌────────────────────────────────────────┐
│ Build & Deploy                         │
├────────────────────────────────────────┤
│ Branch: main                           │  ◄── 確認是 main 分支
│                                        │
│ Root Directory: [留空]                 │  ◄── 使用項目根目錄
│                                        │      不要填寫任何內容
└────────────────────────────────────────┘
```

---

## 步驟 4：配置 Docker 設置（🔴 最關鍵）

### 4.1 Environment 設置

```yaml
┌────────────────────────────────────────┐
│ Environment                            │
├────────────────────────────────────────┤
│ Runtime: [Docker] ◄───────────────────┼── 🔴 必須選擇 Docker
│          └─ Node                       │    （不是 Python！）
│          └─ Python                     │
│          └─ Docker ✓                   │
│          └─ ...                        │
└────────────────────────────────────────┘
```

⚠️ **重要**：必須選擇 **Docker**，不要選擇 Python！

### 4.2 Dockerfile Path（🔴 必須設置）

```yaml
┌────────────────────────────────────────┐
│ Docker Settings                        │
├────────────────────────────────────────┤
│ Dockerfile Path:                       │
│ ┌────────────────────────────────────┐ │
│ │ docker/Dockerfile                  │ │ ◄── 🔴 必須填寫這個
│ └────────────────────────────────────┘ │     （你的截圖中是空的）
│                                        │
│ Docker Context:                        │
│ ┌────────────────────────────────────┐ │
│ │ .                                  │ │ ◄── 填寫一個點（.）
│ └────────────────────────────────────┘ │     表示根目錄
│                                        │
│ Docker Command: [留空]                │  ◄── 留空，使用 Dockerfile CMD
└────────────────────────────────────────┘
```

**你的截圖中的問題**：
- ❌ Dockerfile Path 是空的 → 🔴 **必須填寫** `docker/Dockerfile`
- ❌ 可能沒有選擇 Docker Runtime → 🔴 **必須選擇** `Docker`

### 4.3 Build 和 Start Command

```yaml
┌────────────────────────────────────────┐
│ Build & Deploy Commands                │
├────────────────────────────────────────┤
│ Build Command: [留空]                  │  ◄── 對於 Docker 部署
│                                        │      留空即可
│ Start Command: [留空]                  │  ◄── 使用 Dockerfile CMD
└────────────────────────────────────────┘
```

⚠️ **注意**：對於 Docker 部署，這兩個命令都應該**留空**

---

## 步驟 5：配置環境變量

### 5.1 添加環境變量

點擊 **"Environment"** 標籤，然後點擊 **"Add Environment Variable"**

```yaml
┌────────────────────────────────────────────────────┐
│ Environment Variables                              │
├────────────────────────────────────────────────────┤
│ [+ Add Environment Variable]  ◄── 點擊這裡          │
│                                                    │
│ ┌────────────────┬─────────────────────────────┐  │
│ │ Key            │ Value                       │  │
│ ├────────────────┼─────────────────────────────┤  │
│ │ API_HOST       │ 0.0.0.0                     │  │
│ │ API_PORT       │ 8000                        │  │
│ │ CORS_ORIGINS   │ ["*"]                       │  │
│ │ MODEL_PATH     │ predict/model/best_model... │  │
│ │ DEVICE         │ cpu                         │  │
│ │ SEQ_LENGTH     │ 50                          │  │
│ │ BATCH_SIZE     │ 16                          │  │
│ │ MAX_BATCH_SIZE │ 100                         │  │
│ │ LOG_LEVEL      │ INFO                        │  │
│ │ ENVIRONMENT    │ production                  │  │
│ └────────────────┴─────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

### 5.2 完整環境變量列表

複製以下配置：

| Key | Value | 說明 |
|-----|-------|------|
| `API_HOST` | `0.0.0.0` | API 監聽地址 |
| `API_PORT` | `8000` | API 端口（會被 $PORT 覆蓋） |
| `CORS_ORIGINS` | `["*"]` | 允許所有來源 |
| `MODEL_PATH` | `predict/model/best_model_Oct13.pth` | 模型文件路徑 |
| `DEVICE` | `cpu` | 使用 CPU 推理 |
| `SEQ_LENGTH` | `50` | 最大序列長度 |
| `BATCH_SIZE` | `16` | 批次大小 |
| `MAX_BATCH_SIZE` | `100` | 最大批次大小 |
| `LOG_LEVEL` | `INFO` | 日誌級別 |
| `ENVIRONMENT` | `production` | 生產環境 |

⚠️ **注意**：不需要手動設置 `PORT`，Render 會自動設置為 `10000`

---

## 步驟 6：配置高級設置

### 6.1 Instance Type

```yaml
┌────────────────────────────────────────┐
│ Instance Type                          │
├────────────────────────────────────────┤
│ ○ Starter ($7/month)                   │
│ ● Free ($0/month) ✓  ◄────────────────┼── 選擇免費計劃
│ ○ Standard ($25/month)                 │
└────────────────────────────────────────┘
```

### 6.2 Auto Deploy

```yaml
┌────────────────────────────────────────┐
│ Auto-Deploy                            │
├────────────────────────────────────────┤
│ ☑ Yes  ◄──────────────────────────────┼── 啟用自動部署
│ ☐ No                                   │    （Git push 時自動部署）
└────────────────────────────────────────┘
```

### 6.3 Health Check

```yaml
┌────────────────────────────────────────┐
│ Health Check                           │
├────────────────────────────────────────┤
│ Health Check Path:                     │
│ ┌────────────────────────────────────┐ │
│ │ /health                            │ │ ◄── 填寫健康檢查路徑
│ └────────────────────────────────────┘ │
│                                        │
│ This endpoint will be pinged to       │
│ verify your service is running.       │
└────────────────────────────────────────┘
```

---

## 步驟 7：創建服務並部署

### 7.1 最終確認

在點擊 **"Create Web Service"** 前，確認以下配置：

```yaml
✓ 檢查清單：
├─ ✅ Name: multi-aop-api
├─ ✅ Region: Singapore
├─ ✅ Branch: main
├─ ✅ Root Directory: (空)
├─ ✅ Environment: Docker  🔴 關鍵
├─ ✅ Dockerfile Path: docker/Dockerfile  🔴 關鍵
├─ ✅ Docker Context: .
├─ ✅ Build Command: (空)
├─ ✅ Start Command: (空)
├─ ✅ Environment Variables: 已配置 10 個
├─ ✅ Instance Type: Free
├─ ✅ Auto-Deploy: Yes
└─ ✅ Health Check Path: /health
```

### 7.2 點擊創建

```
┌────────────────────────────────────────┐
│                                        │
│  [Cancel]      [Create Web Service]   │ ◄── 點擊這裡
│                          ↑             │
│                     點擊創建            │
└────────────────────────────────────────┘
```

### 7.3 等待部署

```
部署過程：
├─ 1. Building (構建中) ⏳ 10-15 分鐘
│   ├─ Cloning repository...
│   ├─ Building Docker image...
│   ├─ Installing dependencies...
│   └─ Pushing image...
│
├─ 2. Deploying (部署中) ⏳ 2-3 分鐘
│   ├─ Starting container...
│   ├─ Running health check...
│   └─ Configuring routing...
│
└─ 3. Live (已上線) ✅
    └─ Your service is now live! 🎉
```

---

## 🔍 配置檢查（對照你的截圖）

### 你的截圖中的問題：

| 配置項 | 你的設置 | 應該是 | 嚴重程度 |
|--------|---------|--------|---------|
| **Dockerfile Path** | ❌ 空白 | `docker/Dockerfile` | 🔴 嚴重 |
| **Environment** | ⚠️ 不清楚 | `Docker` | 🔴 嚴重 |
| **Docker Context** | ⚠️ 不清楚 | `.` | 🟡 重要 |
| **Health Check Path** | ⚠️ 不清楚 | `/health` | 🟡 重要 |

### 修正建議：

1. 🔴 **立即修正**：Dockerfile Path
   - 找到 "Dockerfile Path" 字段
   - 填入：`docker/Dockerfile`

2. 🔴 **立即確認**：Environment
   - 找到 "Environment" 或 "Runtime" 下拉選單
   - 確認選擇：`Docker`（不是 Python！）

3. 🟡 **建議設置**：Docker Context
   - 找到 "Docker Context" 字段
   - 填入：`.`（一個點）

4. 🟡 **建議設置**：Health Check Path
   - 找到 "Health Check Path" 字段
   - 填入：`/health`

---

## 🎯 快速配置表（複製貼上）

### 基本設置

```
Name: multi-aop-api
Region: Singapore
Branch: main
Root Directory: [留空]
```

### Docker 設置（🔴 關鍵）

```
Environment: Docker
Dockerfile Path: docker/Dockerfile
Docker Context: .
Build Command: [留空]
Start Command: [留空]
```

### 環境變量（複製貼上）

```
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=["*"]
MODEL_PATH=predict/model/best_model_Oct13.pth
DEVICE=cpu
SEQ_LENGTH=50
BATCH_SIZE=16
MAX_BATCH_SIZE=100
LOG_LEVEL=INFO
ENVIRONMENT=production
```

### 高級設置

```
Instance Type: Free
Auto-Deploy: Yes
Health Check Path: /health
```

---

## ❓ 常見問題

### Q1: 為什麼 Dockerfile Path 必須填寫？

**A**: 因為你的 Dockerfile 不在項目根目錄，而是在 `docker/` 子目錄中。如果不指定路徑，Render 會在根目錄尋找 `Dockerfile`，找不到就會失敗。

---

### Q2: Environment 應該選 Docker 還是 Python？

**A**: 必須選擇 **Docker**！雖然你的項目是 Python 寫的，但你使用的是 Docker 部署方式。選擇 Python 會導致 Render 嘗試直接運行 Python 代碼，而不是構建 Docker image。

---

### Q3: 為什麼 Build Command 和 Start Command 要留空？

**A**: 對於 Docker 部署：
- **Build Command**: Render 會自動執行 `docker build`
- **Start Command**: Render 會使用 Dockerfile 中的 `CMD` 指令

如果你填寫了這些命令，可能會覆蓋 Docker 的默認行為，導致錯誤。

---

### Q4: Docker Context 是什麼？

**A**: Docker Context 是 `docker build` 的上下文目錄，決定了 Dockerfile 中 `COPY` 指令的相對路徑。

```dockerfile
# 在 Dockerfile 中
COPY app/ /app/app/
COPY predict/ /app/predict/

# 這些路徑相對於 Docker Context
# 如果 Context 是 .（根目錄），就會從根目錄複製
# 如果 Context 是 docker/，就會從 docker/ 目錄複製
```

對於這個項目，Context 必須是 `.`（根目錄），因為 Dockerfile 中的 COPY 使用的是根目錄路徑。

---

### Q5: 我的截圖中看不到 Dockerfile Path 字段怎麼辦？

**A**: 可能的原因：
1. **Environment 沒有設置為 Docker**
   - 只有選擇 Docker 後，才會顯示 Docker 相關字段
   - 解決：先選擇 Environment = Docker

2. **頁面還沒有完全加載**
   - 嘗試刷新頁面
   - 或向下滾動查看更多字段

3. **使用的是舊版界面**
   - 嘗試退出並重新登錄
   - 或清除瀏覽器緩存

---

## 🚀 部署後驗證

部署完成後，執行以下檢查：

### 1. 檢查服務狀態

```
在 Render Dashboard 中：
┌─────────────────────────────────────┐
│ multi-aop-api                       │
│                                     │
│ Status: ● Live  ◄─── 應該是綠色     │
│                                     │
│ URL: https://multi-aop-api.onrender │
│      .com                           │
└─────────────────────────────────────┘
```

### 2. 測試 API

```bash
# 替換為你的實際 URL
export API_URL="https://multi-aop-api.onrender.com"

# 健康檢查
curl $API_URL/health

# API 文檔
open $API_URL/docs

# 測試預測
curl -X POST "$API_URL/api/v1/predict/single" \
  -H "Content-Type: application/json" \
  -d '{"sequence": "MKLLVVVFCLVLAAP"}'
```

### 3. 查看日誌

```
在 Render Dashboard 中：
┌─────────────────────────────────────┐
│ multi-aop-api                       │
│                                     │
│ [Overview] [Logs] [Settings]       │
│             ↑                       │
│         點擊這裡查看日誌              │
└─────────────────────────────────────┘

日誌中應該看到：
✅ Model loaded successfully
✅ Application startup complete
✅ Uvicorn running on 0.0.0.0:10000
```

---

## 📚 相關文檔

- [配置修正完整指南](./RENDER_CONFIGURATION_FIX.md)
- [Render 部署完整指南](./RENDER_DEPLOYMENT_GUIDE.md)
- [項目架構分析](./PROJECT_ANALYSIS.md)

---

**祝配置順利！如有問題，請參考上述文檔或查看 Render 日誌。** 🎉

**最後更新**: 2024-12-13

